import React from 'react';
import NotFoundImg from '../../Images/not-found-404.jpg';

const NotFound = () => {
    return (
        <div>
            <img src={NotFoundImg} alt='' />
        </div>
    );
};

export default NotFound;